import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bi.hogi.gestionconge.imagee',
  appName: 'gestion_conge_mobile',
  webDir: 'www.gesionCongeMobile.com',
  server: {
    androidScheme: 'https'
  }
};

export default config;
