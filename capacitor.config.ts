import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.exercice.app',
  appName: 'exercice ionic',
  webDir: 'public',
  server: {
    androidScheme: 'https'
  }
};

export default config;
